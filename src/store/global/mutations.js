// Sets Current Currency
export const SET_CURRENT_CURRENCY = (state, payload) => {
  state.currentCurrency = payload;
};

// Sets Current Sort Direction
export const SET_SORT_DIRECTION = (state, payload) => {
  state.sortDirection = payload;
};
