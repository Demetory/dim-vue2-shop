// Returns Products for Selected Category
export const getCurrentProducts = (state) => state.currentProducts;

// Returns Selected Product
export const getCurrentProduct = (state) => state.currentProduct;
